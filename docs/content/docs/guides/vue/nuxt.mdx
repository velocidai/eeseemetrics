---
title: Nuxt.js
description: Integrate eesee with your Nuxt.js application for analytics tracking
---

import { Steps, Step } from 'fumadocs-ui/components/steps'
import { Callout } from 'fumadocs-ui/components/callout'


This guide explains how to integrate eesee with your Nuxt.js application for analytics tracking. Nuxt.js provides a structured way to add external scripts via its configuration.

## Adding eesee via Nuxt.js Configuration

This is the recommended method for Nuxt.js applications.

<Steps>
<Step>
### Retrieve Your Site ID

Navigate to your eesee dashboard to obtain your Site ID. You'll need this for the `data-site-id` attribute in the script configuration.
</Step>

<Step>
### Modify `nuxt.config.ts` (or `.js`)

**For Nuxt 3:**

Add the eesee script to the `app.head.script` array in your `nuxt.config.ts` file:

```typescript filename="nuxt.config.ts"
export default defineNuxtConfig({
  // ... other configurations
  app: {
    head: {
      script: [
        {
          src: 'https://app.eeseemetrics.com/api/script.js',
          defer: true,
          'data-site-id': 'YOUR_SITE_ID', // Replace with your Site ID
        },
      ],
    },
  },
})
```

**For Nuxt 2:**

Add the eesee script to the `head.script` array in your `nuxt.config.js` file:

```javascript filename="nuxt.config.js"
export default {
  // ... other configurations
  head: {
    // ... other head properties
    script: [
      {
        src: 'https://app.eeseemetrics.com/api/script.js',
        defer: true,
        'data-site-id': 'YOUR_SITE_ID', // Replace with your Site ID
      },
    ],
  },
}
```
Ensure you replace `YOUR_SITE_ID` with your actual Site ID from your eesee dashboard. This method ensures the script is included in the `<head>` of all your pages.
</Step>

<Step>
### Verify Installation

After restarting your Nuxt.js development server or deploying your application, open your eesee dashboard to check if data is being received. You can also inspect your browser's network tab to confirm that `script.js` is loaded.
</Step>
</Steps>

<Callout type="info">
  **SPA Page View Tracking**: eesee's tracking script is designed to automatically detect route changes in Nuxt.js applications and track them as page views. If you find that page views are not being tracked correctly after navigation (which is uncommon for Nuxt), you might need to manually trigger page views using `window.eesee.pageview()` after each route change. You could use Nuxt's router events or middleware for this if necessary, but test the default behavior first.
</Callout>

## Custom Event Tracking

From any Vue component within your Nuxt.js application, you can track custom events:

```vue
<template>
  <button @click="trackCustomEvent">Track Important Action</button>
</template>

<script setup>
function trackCustomEvent() {
  if (process.client && window.eesee) { // Ensure running on client and eesee exists
    window.eesee.event('nuxt_interaction', { detail: 'User clicked important button' });
  }
}
</script>
```
Using `process.client` (Nuxt 2) or checking `typeof window !== 'undefined'` (Nuxt 3, or generally) is good practice before accessing `window` if your component might render or execute logic on the server side.
